pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'Building the application... >> build_logs.txt'
                echo 'Tool: Maven >> build_logs.txt'
            }
        }
        
        stage('Unit and Integration Tests') {
            steps {
                echo 'Running Unit and Integration Tests... >> build_logs.txt'
                echo 'Tools: Maven and JUnit >> build_logs.txt'
            }
            post {
                success {
                    emailext(
                        to: "inaambajawray@gmail.com",
                        subject: "Unit and Integration Tests - Build Successful",
                        body: """
                        Build status: ${currentBuild.currentResult}
                        Check console output: ${env.BUILD_URL}
                        Attached find the logs.
                        """,
                        attachmentsPattern: 'build_logs.txt'                       
                    )
                }
                failure {
                    emailext(
                        to: "inaambajawray@gmail.com",
                        subject: "Unit and Integration Tests - Build Failed",
                        body: """
                        Build status: ${currentBuild.currentResult}
                        Check console output to see logs at: ${env.BUILD_URL}
                        Attached find the logs.
                        """,
                        attachmentsPattern: 'build_logs.txt'
                    )
                }
            }
        }
        
        stage('Code Analysis') {
            steps {
                echo 'Performing Code Analysis... >> build_logs.txt'
                echo 'Tool: SonarQube >> build_logs.txt'
            }
        }
        
        stage('Security Scan') {
            steps {
                echo 'Performing Security Scan... >> build_logs.txt'
                echo 'Tool: OWASP Dependency-Check >> build_logs.txt'
            }
            post {
                success {
                    emailext(
                        to: "inaambajawray@gmail.com",
                        subject: "Security Scan - Successful",
                        body: """
                        Security scan status: ${currentBuild.currentResult}
                        Check console output: ${env.BUILD_URL}
                        Attached find the logs.
                        """,
                        attachmentsPattern: 'build_logs.txt'
                    )
                }
                failure {
                    emailext(
                        to: "inaambajawray@gmail.com",
                        subject: "Security Scan - Failed",
                        body: """
                        Security scan was not successful...
                        Security scan status: ${currentBuild.currentResult}
                        Check console output: ${env.BUILD_URL}
                        Attached find the logs.
                        """,
                        attachmentsPattern: 'build_logs.txt'
                    )
                }
            }
        }
        
        stage('Deploy to Staging') {
            steps {
                echo 'Deploying to Staging Server AWS... >> build_logs.txt'
            }
        }
        
        stage('Integration Tests on Staging') {
            steps {
                echo 'Running Integration Tests on Staging... >> build_logs.txt'
                echo 'Tool: Maven'
            }
        }
        
        stage('Deploy to Production') {
            steps {
                echo 'Deploying to Production Server AWS... >> build_logs.txt'
            }
        }
    }
}
